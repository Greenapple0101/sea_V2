# 로컬 개발 환경 변수 예시
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요

# MySQL 설정
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=sca_db
MYSQL_USER=sca_user
MYSQL_PASSWORD=sca_password
MYSQL_PORT=3306

# 백엔드 환경 변수 (application-local.yaml에서 사용)
DB_NAME=sca_db
DB_USERNAME=sca_user
DB_PASSWORD=sca_password

# JWT 설정
JWT_SECRET=local-dev-secret-key-must-be-at-least-256-bits-long-for-HS256-algorithm-security
JWT_EXPIRATION=3600000
JWT_REFRESH_EXPIRATION=86400000

# OpenAI API 키 (필수)
OPENAI_API_KEY=your-openai-api-key-here

# AWS S3 설정 (선택사항 - 로컬 개발시 더미 값 사용 가능)
AWS_ACCESS_KEY=dummy-access-key
AWS_SECRET_KEY=dummy-secret-key
AWS_S3_BUCKET=dummy-bucket

